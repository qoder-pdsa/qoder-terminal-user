# `make db-migrate` 使用：只执行 Flyway 迁移后退出，不启动 Web 服务
spring.main.web-application-type=none
spring.flyway.enabled=true
# 懒加载：只初始化数据源与 Flyway，业务 bean（JWT 密钥、控制器等）不创建，
# 因此迁移只需要数据库连接，不依赖 USER_JWT_PRIVATE_KEY 等业务配置
spring.main.lazy-initialization=true
